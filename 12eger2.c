#include <stdio.h>

int main(){
//tek satır eğer
/*kısa yol eğer (üçlü operatör) Short Hand If...Else (Ternary Operator)*/
//variable = (condition) ? expressionTrue : expressionFalse;

    int time = 20;
    (time < 18) ? printf("Good day.") : printf("Good evening.");

    return 0;
}
