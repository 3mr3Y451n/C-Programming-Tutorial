#include <stdio.h>

int main(){

    //Bir sabit değişken bildirdiğinizde, ona bir değer atanmalıdır
    const int herSaatIcinDakika = 60;
    const float PI = 3.14;

    /* C dili kullanıcıları için sabit değişkenlerin
    değişken adının tamamının büyük harf ile oluşturulması
    zorunlu olmasa da tavsiye edilir. */
    const int BIRTHYEAR = 1980;

    return 0;
}
